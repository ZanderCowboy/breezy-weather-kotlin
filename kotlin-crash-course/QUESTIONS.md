# Questions

## What is?

- `companion object` -> Static object
- `?:` - Null Safety. `?.`, `?:`, `!!` (Not null), `?.let { }` (only if not null)
- `get()` -> It declaring it as a getter? Yes. A custom getter.

```sh
    val isValid: Boolean
        get() = speed != null && speed > 0
```

- `Serializable`? Interface implementation for data persistence - still unsure.
- `fun`? -> Function?
- `when`? Smart switch statement with pattern matching.
- What are the wrappers? Like `DailyWrapper` and `CurrentWrapper`?
- `Parcelable`?

- What is the difference between the `app` folder and the files in there vs. the files int the `data` or `domain` folder from root?

- `in 22.5..67.5` - Range expressions for number ranges.

- Function

```sh
fun getInstance(value: String?): ReturnType? = null
```

- `Coroutines?`
-
